// See https://containers.dev/implementors/json_reference/ for configuration reference
{
	"name": "Access to Azure via Managed Identity",
	"build": {
		"dockerfile": "Dockerfile"
	},
  "remoteUser": "node"
}